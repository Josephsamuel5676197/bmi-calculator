[project]
name = 'bmi-calculator'
description = ''
authors = []
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.bmi-computation-engine]
path = 'contracts/bmi-computation-engine.clar'
clarity_version = 3
epoch = 3.1

[contracts.health-metrics-analyzer]
path = 'contracts/health-metrics-analyzer.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
